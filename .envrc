#!/usr/bin/env bash
export NIXPKGS_ALLOW_UNFREE=1
# Load the openwrt shell from the shared nix-devshells repo (sibling nix
# meta-workspace). No meta flake.nix here anymore — this dir is a
# tooling-only orchestrator, same pattern as ../nix.
use flake ../nix/nix-devshells#openwrt
